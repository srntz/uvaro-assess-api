type Level {
  level_id: Int!
  level_name: String!
  level_statement: String!
  required_weighting: Int!
  category_id: Int!
  category: Category!
}

type LevelFlat {
  level_id: Int!
  level_name: String!
  level_statement: String!
  required_weighting: Int!
  category_id: Int!
}
